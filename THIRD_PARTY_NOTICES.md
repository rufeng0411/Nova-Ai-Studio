# Third-party notices

Runtime is a Node.js workspace (`ui` + engine). Vendor skills under `skills/vendor` keep their upstream licenses.

This repository is licensed AGPL-3.0. See LICENSE.

# 第三方声明

运行时为 Node 工作区。`skills/vendor` 保留上游许可证。本仓库 AGPL-3.0。
