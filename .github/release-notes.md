## Nova Ai-Studio v2.0.0

Personal SaaS community edition.

```bash
cp .env.example .env
corepack enable
pnpm install
pnpm run dev
```

Open the Vite URL → `/login` → `/app`. Probe `GET /api/saas/health/ready`.

### 中文

个人 SaaS 社区版。登录后进入工作台。
