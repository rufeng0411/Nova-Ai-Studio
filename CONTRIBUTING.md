# Contributing

1. Fork and clone this repository
2. `cp .env.example .env` and set `SAAS_ADMIN_PASSWORD`
3. `corepack enable` then `pnpm install`
4. `pnpm --dir ui run build`

Use Conventional Commits. Do not commit `.env` or secrets.

中文见 [CONTRIBUTING.zh-CN.md](CONTRIBUTING.zh-CN.md)。
