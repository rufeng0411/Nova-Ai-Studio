/**
 * PD-SAAS-FORK: compatibility shim for the shared HTML deliverable rules.
 */
import { isLikelyFormalHtml } from '../../shared/htmlDeliverableRules.mjs';

export const isLikelyRenderableHtmlDeliverable = isLikelyFormalHtml;
